name: Mi Primer Workflow

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  saludar:
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]

    steps:
      - uses: actions/checkout@v3
      - run: echo "¡Hola Mundo!"
      
      - name: Simular deploy (solo en main)
        if: github.ref == 'refs/heads/main'
        run: echo "🚀 Realizando deploy automático desde la rama main..."
